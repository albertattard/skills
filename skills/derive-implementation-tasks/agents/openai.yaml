interface:
  display_name: "Derive Implementation Tasks"
  short_description: "Check readiness and turn slices and release plans into tasks"
  default_prompt: "Use $derive-implementation-tasks to check task readiness, then convert the vertical slices, path-to-production plan, and repository context into ordered implementation task Markdown files."
