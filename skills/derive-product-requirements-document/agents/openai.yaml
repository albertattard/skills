interface:
  display_name: "Derive Product Requirements Document"
  short_description: "Turn agreed scope into a practical PRD"
  default_prompt: "Use $derive-product-requirements-document to convert the agreed scope into a PRD. Ask option-based questions until material PRD decisions are resolved or explicitly deferred."
